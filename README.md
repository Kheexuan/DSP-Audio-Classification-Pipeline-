# DSP Audio Classification Pipeline
# Introduction
The object of this projec is to setup the pipeline for audioc classification, by applying digital signal processing to clean and extract features from the signal in order for the model to perform better.

It will cover the thought behind the decision made as well as the experiements that was carried out along with the results
